# Copy to .env for local overrides. Defaults target the docker-compose Postgres
# (host port 5433 -> 5432 inside the compose network).
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/metacto
TRENDING_GRAVITY=1.5
APP_ENV=development
